% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{create_deseq_dataset}
\alias{create_deseq_dataset}
\title{Create DESeq2 dataset from tximport object}
\usage{
create_deseq_dataset(
  txi,
  col_data,
  level = "transcript",
  design = ~source + treatment + source:treatment,
  min_count = 0,
  min_samples = 0,
  verbose = TRUE
)
}
\arguments{
\item{txi}{tximport object}

\item{col_data}{Column data for samples}

\item{level}{Quantification level used ("transcript" or "gene")}

\item{design}{Design formula (default: ~ source + treatment + source:treatment)}

\item{min_count}{Drop features without at least this many counts in
\code{min_samples} samples. Default 0, which keeps every feature -- apeglm
shrinks a noisy near-zero feature's LFC to 0 but leaves its Wald p-value
untouched, so unfiltered results can carry "significant" features with a
base mean near zero and no effect size.}

\item{min_samples}{Number of samples that must meet \code{min_count}. Only used
when \code{min_count > 0}. Default 0, in which case the smallest cell over the
design's factor columns is used. An explicit value larger than the number
of samples is capped at the sample count, since subset models see fewer
samples than the full dataset.}

\item{verbose}{Print dimensions and size factors (default: TRUE)}
}
\value{
DESeqDataSet object after running DESeq()
}
\description{
Create DESeq2 dataset from tximport object
}
